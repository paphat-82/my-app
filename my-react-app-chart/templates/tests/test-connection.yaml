apiVersion: v1
kind: Pod
metadata:
  name: {{ include "my-react-app-chart.fullname" . }}-test
  labels:
    {{- include "my-react-app-chart.labels" . | nindent 4 }}
spec:
  containers:
    - name: test-connection
      image: busybox
      command: ['sh', '-c', 'echo Hello Kubernetes! && sleep 3600']
